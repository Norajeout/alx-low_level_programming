#include "main.h"
/**
 * positive_or_negative - check the number if positive or negative
 *
 * @i : the number to check
 *
 * Return : always 0 (seccuss)
 */
void positive_or_negative(int i)
{
if (i < 0)
printf("%d is negative\n", i);
else
printf("%d is posetive\n", i);
}
